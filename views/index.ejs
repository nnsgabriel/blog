<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Web Application</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
</head>
<body>
    <h1>Blog Web Application</h1>
    <p>This is the page where all posts are displayed.</p>
    <button class="new-article-button"><a class="button-link" href="/new">New Article</a></button>
    <div class="all-articles-container">
        <% allArticles.forEach((newArticle) => { %>
            <div class="article-container">
                <h2><%= newArticle.title %></h2>
                <p class="createdAt"><%= newArticle.createdAt %></p>
                <p class="article-description"><%= newArticle.description.replace(/\n/g, '<br>') %></p>
                <p><%= newArticle.body.slice(0, 250) %>...</p>
                <button class="button read-more-button"><a class="button-link" href="/read-more/<%= encodeURIComponent(newArticle.title) %>">Read More</a></button>
                <button class="button edit-button"><a class="button-link" href="/edit/<%= encodeURIComponent(newArticle.title) %>">Edit</a></button>
                <form  class="delete-form" action="/delete/<%= encodeURIComponent(newArticle.title) %>" method="POST">
                    <button class="button delete-button" type="submit">Delete</button>
                </form>
            </div>
        <% }); %>
        <%- include("./partials/footer.ejs") %>
    </div>
</body>
</html>